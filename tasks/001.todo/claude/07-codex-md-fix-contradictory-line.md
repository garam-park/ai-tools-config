# 07. codex.md line 10 자기모순 문구 + 오타 정정

- **우선순위**: P2 (문서·기능 일관성)
- **대상 파일**: `global-instructions/codex.md` (L10)
- **상태**: TODO

## 문제

L10: `응답이 길어질 때만 paced-explrainer 청크 모드로 전환 (자동 발동은 기본값)`

한 줄에 두 가지 문제가 있다.

1. **자기모순**: "응답이 길어질 때만 전환"(길이 게이트)과 "자동 발동은 기본값"이 서로 충돌.
   `SKILL.md`(L23)는 자동 트리거가 "가장 강력"하며 "모르겠어" 같은 혼란 신호에 발동한다고 정의 — 길이 조건이 아님.
2. **오타**: `paced-explrainer` → 올바른 이름은 `paced-explainer` (dir/SKILL.md/CLAUDE.md 모두 이 철자).

## 수정 방향

정책 하나를 골라 명확히 서술하고, 오타를 고친다. SKILL.md 정의에 맞추는 안:

```markdown
- paced-explainer는 자동 발동이 기본값이며, 사용자가 이해하지 못한다는 신호("모르겠어" 등)에 청크 모드로 전환한다
```

(수동 전환 정책으로 갈 경우엔 "긴 응답에서만 수동 전환"으로 통일 — 단, 양쪽을 섞지 말 것.)

## 완료 조건

- [ ] 문구가 자기모순 없이 하나의 정책을 서술한다
- [ ] 서술이 SKILL.md의 트리거 정의와 충돌하지 않는다
- [ ] `paced-explrainer` 오타가 `paced-explainer`로 수정됨
