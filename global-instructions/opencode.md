# OpenCode 전용 지침

> OpenCode에서만 추가로 적용되는 사항.
> `~/.config/opencode/AGENTS.md`로 동기화된다.

## OpenCode 전용

- oh-my-openagent 플러그인이 활성화되어 있다. `sisyphus` 에이전트가 기본 작업자다
- 에이전트 모델 변경은 `~/.config/opencode/oh-my-openagent.json`에서만 수행한다 (코드 수정 X)