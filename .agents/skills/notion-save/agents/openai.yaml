interface:
  display_name: "Notion 저장"
  short_description: "지정한 Notion 데이터베이스에 요청 내용을 저장합니다"
  default_prompt: "$notion-save를 사용해 이 내용을 Notion 데이터베이스에 저장해 주세요."

dependencies:
  tools:
    - type: "mcp"
      value: "notion"
      description: "Project-local Notion MCP server"
      transport: "stdio"
