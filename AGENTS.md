# 프로젝트 작업 지침

## 스킬 관리

- 스킬 관리 용어와 판정 기준은 `docs/skill-management.md`를 기준으로 사용한다.
- 스킬을 분류할 때는 소유권, 배포 정책, 배포 상태를 서로 섞지 않는다.
  - 소유권: 프로젝트 관리 / 외부 / 시스템·플러그인
  - 배포 정책: 글로벌 / 프로젝트 전용
  - 배포 상태: 완료 / 부분 / 미배포 / 충돌 / 잔존 / 깨짐
- 글로벌 설치 여부는 스킬 이름만으로 판단하지 말고, 각 설치 대상의 심볼릭 링크가 프로젝트의 올바른 원본을 가리키는지 확인한다.
- 외부 글로벌 스킬과 시스템·플러그인 스킬은 프로젝트 설치 드리프트로 간주하거나 자동 삭제하지 않는다.
- 재현이 필요한 외부 글로벌 스킬은 사용자 요청에 따라 프로젝트 관리 스킬로 가져오고 검증한다.
- 글로벌 배포 스킬의 원본은 `skills/<name>/`, 프로젝트 전용 스킬의 원본은 `.agents/skills/<name>/`에 둔다.
- 프로젝트 전용 스킬을 Claude Code에서도 사용해야 하면 `.claude/skills/<name>`에 `.agents/skills/<name>`을 가리키는 상대 심볼릭 링크를 커밋한다. 내용을 복사해 중복 관리하지 않는다.
- `.github/skills/`와 `.opencode/skills/`에는 같은 프로젝트 스킬을 중복 배치하지 않는다. GitHub Copilot과 OpenCode는 `.agents/skills/`를 직접 읽는다.
- 프로젝트 전용 스킬과 그 호환 링크는 글로벌 설치 manifest에 기록하거나 사용자 홈 경로에 설치하지 않는다.
- Hermes Agent는 리포의 프로젝트 스킬을 자동 탐색하지 않으므로, 글로벌 `external_dirs`에 프로젝트 경로를 추가해 프로젝트 전용 범위를 깨지 않는다. Hermes 지원이 필요하면 별도의 리포 전용 실행 설정을 마련한다.
- 설치 상태 점검은 먼저 `./install-skills.sh doctor`로 수행한다. 사용자 요청 없이 글로벌 링크를 설치하거나 교체하지 않는다.
- 기기의 선택 설치 상태(`install-skills.selection`)를 존중한다. 선택에서 빠진 스킬은 드리프트가 아니므로 사용자 요청 없이 `install --all`로 되돌리지 않는다.
