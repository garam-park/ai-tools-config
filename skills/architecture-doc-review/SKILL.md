---
name: architecture-doc-review
description: 아키텍처/시스템 설계서를 14개 기준(표현·구조·책임·안전)으로 검토해 잘된 설계문서인지 판단합니다. 사용자가 `$architecture-doc-review`를 호출하거나, 설계서를 첨부하고 "이 설계 어때?", "리뷰해줘", "잘됐나?"처럼 평가 의도를 표현할 때 사용합니다. 일반 첨삭·문장 다듬기·번역·코드 리뷰·UX 카피 리뷰에는 사용하지 않습니다.
metadata:
  short-description: 아키텍처 설계서를 14개 기준으로 검토 평가
---

# 아키텍처 설계서 검토 (Architecture Doc Review)

설계서를 받아 14개 기준(표현·구조·책임·안전)으로 평가해 *잘된 설계서인지*를 판단한다. 평가의 근거는 항상 본문·표·도식의 인용으로 제시한다.

## Platforms (Cross-Tool)

원본은 이 리포의 `skills/architecture-doc-review/`이며, `install-skills.sh`가 아래 도구 경로에 리포 작업 트리를 직접 가리키는 심볼릭 링크를 만든다.

- **Claude Code**: `~/.claude/skills/architecture-doc-review/`
- **Codex / GitHub Copilot / OpenCode**: `~/.agents/skills/architecture-doc-review/`

## 트리거

**명시 호출**: `$architecture-doc-review`, "설계서 검토해줘", "이 설계 어때?"

**자동 발동** — 다음 두 조건을 모두 만족하면 사용자가 부르지 않아도 발동한다.

- 아키텍처·시스템·인프라 설계서의 파일 경로 또는 본문이 입력에 있다.
- 사용자가 "어때?", "리뷰", "검토", "잘됐나?" 같은 평가 의도를 함께 표현한다.

PR 직전 상황, 제출자의 경력, 검토해야 한다는 맥락만으로는 발동하지 않는다.

**발동하지 않는 경우** — 잘못된 발동은 오히려 방해가 된다.

- 일반 첨삭·문장 다듬기·번역·문법 교정. (기준 14개와 무관함.)
- 코드 리뷰. (PR diff 검토는 다른 도구.)
- UX 카피·제품 메시지 리뷰. (설계서가 아님.)
- 평가 의도 없이 문서를 단순 공유하는 경우.
- 한 문장·한 문단 수준의 짧은 채팅. (설계서가 아님.)
- 비-아키텍처 문서 (예: 회의록, 학습 노트, 일반 위키).

## 입력

둘 중 어느 것이든 받는다.

1. **파일 경로** — 설계서 파일(주로 `.md`)의 경로를 알려주면 읽어 평가한다.
2. **붙여넣은 마크다운** — 본문 전체를 대화창에 붙여 넣으면 그대로 평가한다.

이미지가 본문에 임베드되어 있어도 *텍스트로 환원 가능한 부분*까지만 평가한다. 도식의 시각적 평가는 범위 밖이다.

## 평가 절차

1. **문서 형태 확인** — 마크다운 본문인지, 다이어그램이 들어 있는지, 표가 있는지 빠르게 훑는다. 설계서가 아니면 [설계서가 아닐 때](#설계서가-아닐-때)의 짧은 거절 응답으로 종료한다.
2. **14개 기준 적용** — [references/rubric.md](references/rubric.md)의 기준을 순서대로 적용한다. 각 기준은 *통과 / 부분 / 미흡* 중 하나로 판정한다.
3. **프레임워크 보조 검증** — [references/frameworks.md](references/frameworks.md)에서 문서 유형에 해당하는 프레임워크만 골라 *내용 누락 후보*를 점검한다. 해당하지 않는 프레임워크는 `해당 없음`으로 남긴다.
4. **근거 수집** — 각 판정에는 [근거 위치 표기](#근거-위치-표기) 규칙에 따라 위치를 첨부한다. 추측만으로 단정하지 않는다.
5. **종합 판정** — 14개 중 어디가 가장 약한지, 어떤 묶음(표현/구조/책임·안전)이 깨졌는지 한 문장으로 정리한다.

## 결과 형태

다음 순서로 보고한다. 길이보다 **판정의 근거**를 우선한다.

### 1. 종합 판정 (한 줄)

> *예: "표현·구조는 안정적이나 책임·안전 묶음에서 소유·식별자 기준이 어긋나 재작업이 필요함."*

### 2. 14개 기준 점검표

| #   | 기준              | 판정              | 근거 위치             |
| --- | ----------------- | ----------------- | --------------------- |
| 1   | 메타-서술 금지    | 통과 / 부분 / 미흡 | `path:line`, `§2 > 논리 구성`, `표 3-2` 등 |
| ... | ...               | ...               | ...                   |

(14개 모두 한 줄씩 채운다.)

### 3. 상위 이슈 3~5개

심각도순으로 나열한다. 각 이슈는 *어떤 기준이* 어긋났는지, *어디서* 어긋났는지를 적는다.

| 심각도 | 판정 기준 |
| ------ | --------- |
| 상     | 다른 사람이 설계서만 보고 운영·구현·승인하기 어렵거나, 소유·보안·경계 오해로 재작업이 예상되는 경우 |
| 중     | 용어·근거·범위가 불명확해 검토자가 추가 확인 없이는 판단하기 어려운 경우 |
| 하     | 이해를 막지는 않지만 일관성이나 명확성을 떨어뜨리는 경우 |

| 심각도 | 기준 | 위치   | 한 줄 설명 |
| ------ | ---- | ------ | ---------- |
| 상     | #11  | 표 3-2 | 수동/자동 경계 미명시 |

### 4. 자주 빠지는 5개 항목 체크

체크리스트 5개 ([references/rubric.md §6](references/rubric.md))를 한 번 더 훑는다.

- [ ] 도식이 한 명제인가?
- [ ] 미정 표시가 명시적인가?
- [ ] 자원의 소유 주체가 비어 있지 않은가?
- [ ] 책임 경계(음의 공간)가 적혀 있는가?
- [ ] 식별자와 별칭이 같은 줄에 섞여 있지 않은가?

### 5. (선택) 1~2줄 개선 제안

기본은 식별만 한다. 사용자가 *"어디를 어떻게 고치면 돼?"*라고 물으면 1~2줄로 어디를 어떻게 고치면 되는지 제안한다. 본문 전체를 새로 쓰지 않는다. 사용자가 직접 고친다.

### 6. 근거 위치 표기

- **파일 입력**: `path:line` 또는 `path:line-range`를 쓴다.
- **붙여넣은 입력**: 가장 짧은 원문 인용과 함께 가장 가까운 헤딩 경로, 표 번호 또는 도식 노드명을 쓴다.
- **도식**: 노드·선·라벨 같은 관찰 가능한 요소를 인용하고, 렌더링 결과의 미적 품질은 평가하지 않는다.

## 설계서가 아닐 때

14개 기준표를 만들지 않고 다음 세 항목만 답한다.

1. 설계서로 보지 않은 근거
2. 평가하지 않은 부분
3. 코드 리뷰, 문서 첨삭, UX 카피 리뷰처럼 더 적합한 검토 종류

## 하지 않는 것

- **문장 다듬기·교정** — 평가는 하지만 수정은 사용자가 직접 한다.
- **새 기준 추가** — 14개 외 기준(예: 팀 컨벤션, 회사 정책)은 적용하지 않는다. 별도 요청 시에만 다룬다.
- **전 프레임워크 강요** — 모든 문서에 7개 프레임워크를 적용하지 않는다. 문서 유형에 맞는 항목만 누락 후보로 본다.
- **의도 추측** — 작성자의 의도를 짐작으로 평가하지 않는다. *글에 적힌 것*만 평가한다.
- **도식 시각 평가** — 다이어그램이 *무엇을 보여주는지*는 평가하지만, *예쁘게 그려졌는지*는 평가하지 않는다.
- **점수 매기기** — 100점 만점 같은 종합 점수를 내지 않는다. *통과/부분/미흡*과 *상위 이슈*만 보고한다.

## 원전

- vault 정리본: `00 Inbox/설계 잘하기/01-설계-프레임워크-7종.md`, `00 Inbox/설계 잘하기/02-내용적-기준-14개.md`
- vault 강의: `00 Inbox/설계 잘하기/강의/`
- 이 스킬의 `references/`는 위 vault 정리본의 작업용 사본이다. 원전이 갱신되면 사본도 갱신해야 한다.
